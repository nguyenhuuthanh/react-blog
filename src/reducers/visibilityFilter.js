import { SET_FILTER } from './actionTypes';

export default (state = [], action) => {
  switch (action.type) {
    case SET_FILTER: {
      return action.payload.filter;
    }
    default: {
      return state;
    }
  }
};